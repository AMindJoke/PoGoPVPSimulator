const assert = require("node:assert/strict");
const fs = require("node:fs");
const path = require("node:path");

const html = fs.readFileSync(path.join(__dirname, "..", "PogoPvp.html"), "utf8");

assert.match(html, /src="src\/team-builder\/team-builder-state\.js"/);
assert.match(html, /src="src\/team-builder\/team-builder-meta\.js"/);
assert.match(html, /src="src\/team-builder\/team-builder-analysis\.js"/);
assert.match(html, /src="src\/team-builder\/team-builder-share\.js"/);
assert.match(html, /id="teamBuilderTab"[^>]+data-view-target="team-builder"/);
assert.match(html, /id="teamBuilderView" class="app-view team-builder-view"/);
assert.match(html, /id="teamBuilderRoster" class="team-roster-grid"/);
assert.match(html, /id="teamBuilderPickerModal"[^>]+role="dialog"[^>]+aria-modal="true"/);
assert.match(html, /id="teamBuilderEditorModal"[^>]+role="dialog"[^>]+aria-modal="true"/, "Build editing must use a focused accessible modal.");
assert.match(html, /id="teamBuilderFastMove"/);
assert.match(html, /id="teamBuilderChargedMove1"/);
assert.match(html, /id="teamBuilderChargedMove2"/);
assert.match(html, /data-team-build-profile="default"[\s\S]{0,300}data-team-build-profile="rank1"[\s\S]{0,300}data-team-build-profile="custom"/);
assert.match(html, /id="teamBuilderShadowToggle"[^>]+aria-pressed="false"/);
assert.match(html, /id="teamBuilderMeta"[^>]+aria-label="Team Builder meta"/);
assert.match(html, /id="teamBuilderShieldScenarios"[^>]+role="group"[^>]+aria-label="Shield scenario"/);
assert.match(html, /id="teamBuilderMetaList" class="team-meta-list"/);
assert.match(html, /id="teamBuilderAnalysisProgress"[^>]+role="progressbar"/);
assert.match(html, /id="teamBuilderAnalyze"/);
assert.match(html, /id="teamBuilderAnalysisCancel"/);
assert.match(html, /id="teamBuilderThreatCoverage" class="team-coverage is-primary"[^>]+aria-labelledby="teamBuilderThreatCoverageTitle"/);
assert.match(html, /id="teamBuilderCoverage" class="team-coverage is-secondary"/);
assert.match(html, /id="teamBuilderCoverageDesktop" class="team-coverage-desktop"/);
assert.match(html, /id="teamBuilderThreatCoverageDesktop" class="team-coverage-desktop"/);
assert.ok(html.indexOf('id="teamBuilderThreatCoverage"') < html.indexOf('id="teamBuilderCoverage"'), "Threats Coverage must be presented before the secondary Meta Coverage matrix.");
assert.match(html, /id="teamBuilderMobileOpponent" class="team-mobile-opponent-current"/);
assert.match(html, /id="teamBuilderMobileMatchups" class="team-mobile-matchups"/);
assert.match(html, /id="teamBuilderThreatMobileMatchups" class="team-mobile-matchups"/);
assert.match(html, /id="teamBuilderMatchupModal"[^>]+role="dialog"[^>]+aria-modal="true"/);
assert.match(html, /id="teamBuilderInsights" class="team-insights"[^>]+aria-labelledby="teamBuilderInsightsTitle"/);
assert.match(html, /id="teamBuilderThreatList" class="team-insight-list"/);
assert.match(html, /id="teamBuilderCoveredList" class="team-insight-list"/);
assert.match(html, /id="teamBuilderWeakCoreList" class="team-core-list"/);
assert.match(html, /id="teamBuilderFragileAnswerList" class="team-core-list"/);
assert.match(html, /id="teamBuilderReplacement" class="team-replacement"[^>]+aria-labelledby="teamBuilderReplacementTitle"/);
assert.match(html, /id="teamBuilderReplacementMembers" class="team-replacement-members"/);
assert.match(html, /id="teamBuilderReplacementThreats" class="team-replacement-threats"/);
assert.match(html, /id="teamBuilderReplacementProgress"[^>]+role="progressbar"[^>]+aria-label="Replacement candidate analysis"/);
assert.match(html, /id="teamBuilderReplacementRanking" class="team-replacement-ranking"/);
assert.match(html, /id="teamBuilderComparison" class="team-comparison"[^>]+aria-labelledby="teamBuilderComparisonTitle"/);
assert.match(html, /id="teamBuilderComparisonTeamA"/);
assert.match(html, /id="teamBuilderComparisonTeamB"/);
assert.match(html, /id="teamBuilderComparisonProgress"[^>]+role="progressbar"[^>]+aria-label="Team comparison analysis"/);
assert.match(html, /id="teamBuilderComparisonGains" class="team-comparison-change-list"/);
assert.match(html, /id="teamBuilderComparisonLosses" class="team-comparison-change-list"/);
assert.match(html, /id="teamBuilderCopyLink"[^>]+type="button"[^>]*>Copy Team Link</);
assert.match(html, /id="teamBuilderShareFeedback"[^>]+role="status"[^>]+aria-live="polite"/);
assert.match(html, /id="teamBuilderShareError"[^>]+role="alert"[^>]+hidden/);
assert.match(html, /\.team-builder-share-error\[hidden\] \{ display: none; \}/, "The invalid-link recovery panel must remain absent until a load actually fails.");
assert.match(html, /function renderTeamBuilderSlot\(member, slot\)[\s\S]{0,700}data-team-add/);
assert.match(html, /teamBuilderState\.team\.map\(renderTeamBuilderSlot\)/, "The UI must render the six semantic state slots.");
assert.match(html, /renderPokemonSuggestionList\(\$\("teamBuilderPickerResults"\)/, "Team Builder must reuse the simulator Pokemon result component.");
assert.match(html, /teamBuilderDefaultMember[\s\S]{0,700}metaMovesForPokemon\(pokemon\)/, "Cards must use the existing default competitive moveset resolution.");
assert.match(html, /function teamBuilderResolvedBuild[\s\S]{0,1400}rankingsForPokemon\(pokemon\)[\s\S]{0,1400}statsForIvSpread\(pokemon/, "Team Builder must reuse the simulator's IV optimization and stat calculation.");
assert.match(html, /function saveTeamBuilderEditor[\s\S]{0,1200}PvPeakTeamBuilder\.setMember/, "Moves and builds must commit atomically to semantic state.");
assert.match(html, /function toggleTeamBuilderShadow[\s\S]{0,1400}teamBuilderShadowCounterpart/, "Shadow editing must use existing Pokemon form records.");
assert.match(html, /charged\.length !== 2/, "The editor must require two distinct Charged Moves.");
assert.match(html, /function loadTeamBuilderMeta[\s\S]{0,1200}teamBuilderMetaRegistry\.load/, "Meta definitions must load through the provider registry.");
assert.match(html, /function setTeamBuilderShieldScenario[\s\S]{0,500}setAnalysisConfig/, "Shield scenario must persist in semantic Team Builder state.");
assert.match(html, /Matchups are not calculated yet/, "Phase 3 must clearly avoid presenting definition data as completed analysis.");
assert.match(html, /function createTeamBuilderBattleConfig[\s\S]{0,900}createTeamBuilderCombatant[\s\S]{0,300}createMetaCombatant/, "Team × Meta jobs must reuse canonical combatant builders.");
assert.match(html, /source: "team-builder"[\s\S]{0,300}includeSwing: false[\s\S]{0,300}config/, "Team analysis must route canonical configurations through the existing matrix worker.");
assert.match(html, /function handleMatrixComputeWorkerMessage[\s\S]{0,500}handleTeamBuilderComputeWorkerMessage/, "The existing worker result path must route Team Builder jobs semantically.");
assert.match(html, /function simulateTeamBuilderMatchupCell[\s\S]{0,1600}runAutomaticBattleToEnd\(1000\)[\s\S]{0,300}scoreShieldMatrixResult/, "Fallback computation must still use the canonical automatic engine and scoring.");
assert.match(html, /All \$\{total\} deterministic matchups are ready from cache/, "Phase 4 must report prepared engine data without rendering the Phase 5 matrix.");
assert.match(html, /function teamBuilderThreatOpponentIds\(\)[\s\S]{0,1000}TEAM_BUILDER_THREAT_POOL_SIZE/, "Threat coverage must extend the selected meta with the local ranked competitive field.");
assert.match(html, /function teamBuilderCombinedCoveragePlan\(\)[\s\S]{0,300}teamBuilderThreatAnalysisPlan[\s\S]{0,150}teamBuilderAnalysisPlan/, "Meta and broader threat jobs must share one deduplicated canonical analysis run.");
assert.match(html, /function teamBuilderResultVisual\(result\)[\s\S]{0,2200}#777772[\s\S]{0,350}#277a43[\s\S]{0,650}#b33b4d/, "The seven-level scale must include a calm gray midpoint and balanced green and red endpoints.");
assert.match(html, /--result-surface:\$\{palette\.surface\}[\s\S]{0,180}--result-text:\$\{palette\.text\}[\s\S]{0,180}--gauge-track:\$\{palette\.track\}/, "Every result tier must coordinate surface, text, and gauge contrast.");
assert.match(html, /function teamBuilderGaugeHtml\(score\)[\s\S]{0,350}\/ 1000 \* 270[\s\S]{0,250}class="team-matchup-gauge"/, "The compact gauge must map the full 0–1000 battle rating continuously onto its arc.");
assert.equal((html.match(/class="team-coverage-scale-bar"/g) || []).length, 2, "Both coverage matrices must explain the same seven-level palette.");
assert.match(html, /Seven result levels from dominant loss, clear loss, slight loss, neutral, slight win, clear win, to dominant win/, "The compact visual scale must remain fully accessible.");
assert.match(html, /\.team-matchup-gauge::before[^}]+conic-gradient[^}]+--gauge-angle/, "Coverage gauges must be rendered as lightweight code-native arcs.");
assert.match(html, /class="team-coverage-rank">\$\{row \+ 1\}/, "Coverage rows must expose a clear visual rank.");
assert.doesNotMatch(html, /<strong>\$\{gauge\}\$\{result\.score\}<\/strong><small>\$\{label\}/, "Desktop cells must not repeat Win or Loss beneath every score.");
assert.match(html, /function renderTeamBuilderCoverageDesktop[\s\S]{0,4200}team-coverage-table/, "Desktop coverage must render a six-column semantic table.");
assert.match(html, /function renderTeamBuilderCoverageMobile[\s\S]{0,2200}teamBuilderMobileOpponentIndex/, "Mobile coverage must use a focused opponent presentation.");
assert.match(html, /function openTeamBuilderMatchup[\s\S]{0,2600}Battle rating[\s\S]{0,1000}teamBuilderBuildLabel/, "Every prepared cell must expose simulation and build detail.");
assert.match(html, /aria-label="\$\{escapeHtml\(accessible\)\}"/, "Matrix cells must expose text-based result labels for accessibility.");
assert.match(html, /function renderTeamBuilderInsights\(groups\)[\s\S]{0,500}analyzeCoverage\(groups\)/, "Team insights must derive from the deterministic coverage model.");
assert.match(html, /insights\.threats\.slice\(0, 5\)[\s\S]{0,200}insights\.bestCovered\.slice\(0, 5\)/, "Threat and best-covered summaries must remain intentionally compact.");
assert.match(html, /No favorable matchup[\s\S]{0,300}hard losses[\s\S]{0,100}Avg/, "Threat rows must explain missing answers and hard losses without relying on severity color.");
assert.match(html, /function renderTeamBuilderCoreAnalysis\(groups\)[\s\S]{0,500}analyzeCores\(groups, teamBuilderState\.team\.length\)/, "Core Analysis must derive from the deterministic analysis module.");
assert.match(html, /analysis\.weakCores\.slice\(0, 5\)[\s\S]{0,200}analysis\.fragileAnswers\.slice\(0, 5\)/, "Core summaries must remain compact and ranked.");
assert.match(html, /Only \$\{answer\?\.name[\s\S]{0,300}Best backup/, "Fragile answers must identify both the sole answer and best simulated backup.");
assert.match(html, /function prepareTeamBuilderReplacementPlan\(\)[\s\S]{0,2200}createPlan\([\s\S]{0,500}replacementCandidateId/, "Replacement candidates must use the canonical cached matchup plan.");
assert.match(html, /function renderTeamBuilderReplacementRanking\(\)[\s\S]{0,1800}rankReplacementCandidates/, "Replacement ranking must come from the transparent analysis model.");
assert.match(html, /hard losses fixed[\s\S]{0,200}answers gained[\s\S]{0,200}new hard losses/, "Replacement results must expose the scoring tradeoffs.");
assert.match(html, /Ranking score = average rating delta \+ 40 per hard loss fixed \+ 25 per favorable answer gained − 50 per new hard loss/, "Replacement scoring must be transparent to the user.");
assert.match(html, /function applyTeamBuilderReplacement\(candidateId\)[\s\S]{0,700}PvPeakTeamBuilder\.setMember/, "A selected recommendation must update the canonical Team Builder state.");
assert.match(html, /function captureTeamBuilderComparisonBaseline\(renderNow = true\)[\s\S]{0,700}persistTeamBuilderComparisonBaseline/, "Team A must be a persistent semantic roster snapshot.");
assert.match(html, /function prepareTeamBuilderComparisonPlan\(\)[\s\S]{0,900}createTeamBuilderPlanForTeam\(teamBuilderComparisonBaseline\)[\s\S]{0,300}uniqueJobs/, "Team comparison must reuse and deduplicate canonical matchup jobs.");
assert.match(html, /function renderTeamBuilderComparisonResults\(\)[\s\S]{0,1600}compareTeamCoverage\(groupsA, groupsB\)/, "Comparison results must derive from the deterministic analysis model.");
assert.match(html, /Coverage rating[\s\S]{0,500}Average rating[\s\S]{0,500}Strong answers[\s\S]{0,500}Unhandled threats/, "Comparison must expose its principal coverage deltas.");
assert.match(html, /if \(!teamBuilderComparisonBaseline\) captureTeamBuilderComparisonBaseline\(false\)/, "Applying a suggested replacement must preserve the original roster automatically.");
assert.match(html, /function teamBuilderSharePayload\(\)[\s\S]{0,220}comparisonBaseline: teamBuilderComparisonBaseline/, "Share links must include the optional comparison baseline.");
assert.match(html, /function copyTeamBuilderShareLink\(\)[\s\S]{0,800}buildTeamUrl\([\s\S]{0,300}writeManualScenarioLinkToClipboard/, "Team links must reuse the existing resilient clipboard flow.");
assert.match(html, /function loadSharedTeamFromLocation\(\)[\s\S]{0,500}validateSharedTeamForRuntime\(shareApi\.decodeTeam\(token\)\)/, "Shared teams must decode and pass runtime validation before state is replaced.");
assert.match(html, /function loadSharedTeamFromLocation\(\)[\s\S]{0,1100}setAppView\("team-builder"\)/, "A valid or invalid Team Builder link must open the correct workspace.");
assert.match(html, /function dismissTeamBuilderShareError\(\)[\s\S]{0,400}locationWithoutTeam/, "Invalid links must offer a recoverable path that clears only the share hash.");
assert.match(html, /void loadSharedTeamFromLocation\(\)/, "Shared teams must be restored during application startup.");
assert.match(html, /#teamBuilderView \.team-insights \{[^}]*max-width: none/, "Global section sizing must not compress Team Insights.");
assert.match(html, /@media \(max-width: 760px\)[\s\S]{0,3600}team-insights-grid[^}]*grid-template-columns: 1fr/, "Team Insights must stack cleanly on mobile.");
assert.match(html, /@media \(max-width: 760px\)[\s\S]{0,3800}team-core-grid[^}]*grid-template-columns: 1fr/, "Core Analysis must stack cleanly on mobile.");
assert.match(html, /@media \(max-width: 760px\)[\s\S]{0,4300}team-replacement-steps[^}]*grid-template-columns: 1fr/, "Replacement controls must stack cleanly on mobile.");
assert.match(html, /@media \(max-width: 760px\)[\s\S]{0,4700}team-comparison-changes[^}]*grid-template-columns: 1fr/, "Team comparison changes must stack cleanly on mobile.");
assert.match(html, /@media \(max-width: 760px\)[\s\S]{0,5200}team-builder-share[^}]*min-height: 44px/, "The share control must remain a comfortable mobile target.");
assert.match(html, /Species Clause: that Pokemon species is already on your team/);
assert.match(html, /@media \(max-width: 760px\)[\s\S]{0,900}team-roster-grid[\s\S]{0,100}repeat\(2/ , "Common mobile widths must use two comfortable columns.");
assert.match(html, /@media \(max-width: 760px\)[\s\S]{0,1800}team-editor-moves[\s\S]{0,80}1fr/, "The mobile build editor must stack move controls instead of compressing them.");
assert.match(html, /@media \(max-width: 430px\)[\s\S]{0,180}team-roster-grid[\s\S]{0,80}1fr/, "Very narrow mobile widths must use one column.");
assert.match(html, /#teamBuilderEditorModal \.team-editor-section \{[^}]*max-width: none/, "Global section sizing must not compress the build editor.");
assert.doesNotMatch(html, /team-builder-placeholder/);
assert.match(html, /@media \(max-width: 760px\)[\s\S]{0,3200}team-coverage-desktop[\s\S]{0,100}display: none[\s\S]{0,300}team-coverage-mobile[\s\S]{0,100}display: block/, "Mobile must not compress the desktop matrix.");

console.log("Team Builder UI contract tests passed.");
