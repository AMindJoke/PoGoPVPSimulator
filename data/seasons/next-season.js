(function (root) {
  "use strict";

  // Canonical preview entry point. Keep null until every numeric override and all
  // season-derived outputs are supplied and validated. See SEASON_PREVIEW_WORKFLOW.md.
  root.BATTLE_NEXT_SEASON = null;
})(typeof globalThis !== "undefined" ? globalThis : this);
